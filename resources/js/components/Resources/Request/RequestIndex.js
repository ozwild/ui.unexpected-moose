import React from 'react';
import {Container} from "semantic-ui-react";
import RequestIndexTable from "./RequestIndexTable";

const RequestIndex = ()=>{
    return (
        <Container>
            <RequestIndexTable/>
        </Container>
    );
};

export default RequestIndex;